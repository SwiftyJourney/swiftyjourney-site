---
type Props = {
  eyebrow?: string;
  title: string;
  lede?: string;
  align?: "left" | "center";
  as?: "h2" | "h3";
};

const { eyebrow, title, lede, align = "left", as = "h2" } = Astro.props;
const alignClass = align === "center" ? "text-center mx-auto" : "text-left";
const Tag = as;
---

<header class={`max-w-3xl ${alignClass}`}>
  {eyebrow && (
    <p class="text-xs font-semibold uppercase tracking-[0.15em] text-coral-500 mb-3">
      {eyebrow}
    </p>
  )}
  <Tag class="text-4xl md:text-5xl font-semibold tracking-tight text-text">
    {title}
  </Tag>
  {lede && (
    <p class="mt-4 text-lg text-text-muted leading-relaxed">
      {lede}
    </p>
  )}
</header>
